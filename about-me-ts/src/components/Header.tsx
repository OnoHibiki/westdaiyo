import React from "react";

const Header: React.FC = () => {
    return(
        <header style={{ background: '#222', color:'#fff', padding: '1rem'}}>
            <h1>Hibiki'sのポートフォリオサイト</h1>
        </header>
    );
};

export default Header;